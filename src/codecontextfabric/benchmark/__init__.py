"""Code Context Fabric benchmark infrastructure — measurement only.

This package never modifies parsing/resolution behavior; it observes the
existing `Indexer`/`GraphStore`/`SearchEngine` and reports honest numbers,
including where Code Context Fabric falls short of published third-party claims. See
docs/benchmark-methodology.md.
"""
